1. loss的值是否有代表性?
  每一次selfplay所产生的数据量不同, loss随之而发生变化, 是否需要依据数据量做平均化? 而平均后的loss是否过小?成个放大倍数呢?

2. 维护 best_model, 当前网络每过iters_per_eval次迭代就与best_model进行pk, 并行进行eval_rounds场比赛, 如果新模型胜率大于55%, 
就把best_model更新为新网络参数, 否则训练回退

3. 随机性相关的参数设置
T,alpha,epsilon如何设置? 分别代表什么? 大小有什么含义? 大概在哪个范围?
如何随训练次数增加而缩小(退火)?

4. 模型参数如何选择?
网络架构:
    残差块数, 通道数
并行设置:
    并行游戏数, SP局数, batch_size
训练设置:
    学习率, wd, 迭代次数, 搜索次数, eval间隔
随机性:
    温度, epsilon, alpha

5. 显示各部分耗时占比, 我需要分析性能瓶颈

6. MCTS部分较慢, 能否使用多线程的方式进行加速?

7. 网络架构采用mHC进行改进可能吗?